#Write a program that calculates the area of a rectangle. Add appropriate comments explaining each line of code.
l = float(input('Enter the length of rectangle, l : '))
b = float(input("Enter the breadth of rectangle, b: "))
area = ("Area of rectangle = ",l*b)
